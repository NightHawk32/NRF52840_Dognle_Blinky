{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "nrf-connect",
			"request": "launch",
			"name": "Launch build",
			"config": "${workspaceFolder}/build",
			"runToEntryPoint": "main",
			"serverArgs": "-if swd -device ${device} -speed 6000 -port ${port} -singlerun -nogui -halt -noir -silent -rtos ${rtosPlugin}"
		}
	]
}